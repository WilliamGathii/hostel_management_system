const pool = null;

const testDatabaseConnection = async () => false;

const closeDatabaseConnection = async () => {};

module.exports = {
  pool,
  testDatabaseConnection,
  closeDatabaseConnection,
};
